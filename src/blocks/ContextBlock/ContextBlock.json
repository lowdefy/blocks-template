{
  "category": "container",
  "loading": false,
  "schema": {
    "properties": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string",
          "description": "ContextBlock content string, alternatively provide and list of blocks as ContextBlock content."
        },
        "hideActionLoading": {
          "type": "boolean",
          "description": "Hide loading spinner when block actions are executing."
        },
        "style": {
          "type": "object",
          "description": "Style to apply to ContextBlock div."
        },
        "title": {
          "type": "string",
          "description": "Title to render in ContextBlock."
        }
      }
    },
    "actions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "onClick": {
          "type": "array"
        }
      }
    }
  }
}
